## v1.0.3 — 2025-04-05

### Changed
- Translated all PHP comments and inline strings from Italian to English across all source files
- Removed internal reference codes (RB-XX, Sezione X) from comments
- Admin UI labels, error messages and console log strings now in English
- `lang="it"` → `lang="en"` in admin HTML templates
- `toLocaleTimeString('it-IT')` → `'en-GB'` in JS files

### Fixed
- No functional changes — this release is a code quality / internationalisation cleanup only